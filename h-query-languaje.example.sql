create table Usuario (
  id integer primary key auto_increment,
  nombre varchar(255) unique,
  contrasenya varchar(2048),
  id_plan integer,
  foreign key (id_plan) references Plan (id)
);

create table Usuario (
  id integer primary key auto_increment,
  nombre varchar(255) unique,
  contrasenya varchar(2048),
  id_plan integer,
  foreign key (id_plan) references Plan (id)
);

create table Usuario (
  id integer primary key auto_increment,
  nombre varchar(255) unique,
  contrasenya varchar(2048),
  id_plan integer,
  foreign key (id_plan) references Plan (id)
);

create table Usuario (
  id integer primary key auto_increment,
  nombre varchar(255) unique,
  contrasenya varchar(2048),
  id_plan integer,
  foreign key (id_plan) references Plan (id)
);